\chapter{Implementation and Results} 
\label{chap:implementation_results} 
%TODO: exact tool commands, for example the Nmap \texttt{-sS -sV -p- -T3} invocation, should be listed in an appendix 
%TODO ist es möglich weniger subsctions zu machen, damit das Inhaltsverzeichnis nicht über 2 1/2 Seiten geht (wie jetzt), sondern maximal über 2 
This chapter applies the methodology of Chapter \ref{chap:methodology} to a physical System Under Test. It documents the implemented environment, the application of the method, the collected evidence, the verification of the derived requirements, the validation of the methodology against the thesis objective, and the reliability of the findings.

The practical work covers the direct-access variant NV1 defined in Section \ref{subsec:hybrid_approach}. The passive-observation variant NV2 and the in-path variant NV3 are part of the methodology but are not realised in the laboratory. The methodology is therefore validated only in part. Its central steps are exercised on a real device, whereas the tests that depend on NV2 or NV3 remain specified but not executed.

The chapter follows the evidence-first principle of Section \ref{subsec:verdicts}. An activity is reported as performed only when a stored artifact documents it. A tool execution is not treated as requirement verification. An observation is kept separate from its interpretation. Expected behaviour from a test specification is not presented as an observed result.


\section{Practical Scope and Completion Status}
\label{sec:practical_scope}

The demonstration applied the methodology to the Safe Remote I/O under the NV1 network position defined in Section \ref{subsec:hybrid_approach}. The test system acted as a direct IP participant on the shared switch. The passive-observation variant NV2 and the in-path variant NV3 were not realised. No mirror or SPAN port was configured, because the available laboratory switch does not support port mirroring, and the test system was never placed inline, because rebuilding the setup for an inline bridge exceeded the available time. The cyclic PROFINET and PROFIsafe communication between the controller and the SUT was therefore not observed, and no claim is made about that traffic.

The scope of the practical validation must be distinguished from the achievement of the thesis objective. The objective is to develop and validate a systematic methodology for verifying security functions, not to execute every technically possible test. The methodology was applied on a real device, which exercises its central steps. The empirical coverage was bounded by the single test object, the implemented topology, the available interfaces, and the NV1-compatible activities.

Within NV1, the evaluation focused on the IoT-Core HTTP interface and on tests in which the test system acted as an unauthorised communication partner. Where a company tool assumed an interface that the device does not expose, the test was recorded as an applicability finding rather than as a failure, following Section \ref{subsec:company_framework}.


\section{System Under Test and Test Environment}
\label{sec:testbed_setup}

\subsection{Test Object} 
\label{subsec:test_object} 

The test object is the Safe Remote I/O (SRIO), a fieldbus-based safety gateway for industrial automation systems. The device transmits safety-related input and output data between field devices and a higher-level safety controller over PROFINET and PROFIsafe. The evaluated unit identified itself over the IoT-Core interface as product \texttt{ifm-AL400S}, hardware version 1.4.0.2, hardware revision AA, and software revision 1.1.4.888. All tests were performed on this configuration.

Based on the asset classification introduced in Section \ref{subsec:assets_attack_surfaces}, the SRIO consists of hardware, software, and data assets. The hardware assets include the communication module (COM), the safety CPU module (SCPU), non-volatile memory components, and the physical enclosure. Externally accessible hardware interfaces comprise the Ethernet port, power supply connectors, safe digital input and output interfaces, rotary switches, and status LEDs. Internal interfaces include the SysCom connection between the COM and SCPU modules, inter-processor communication channels, and JTAG debugging interfaces. 

The software assets consist of the COM firmware, SCPU firmware, bootloader, and NetX90 firmware. In addition, the device provides an embedded IoTCore service, a PROFINET communication stack, a PROFIsafe implementation, and a firmware update mechanism. These software components provide network connectivity, device management, diagnostic functions, and safety-related communication services. 

The data assets include network configuration parameters, PROFIsafe configuration data, iPar parameters, firmware images, process data, diagnostic information, and device identity information such as serial number, hardware version, and software version. Configuration and safety-related parameters are stored in non-volatile memory and are therefore considered critical assets. From a security perspective, the primary interfaces are the HTTP-based IoTCore web interface, the associated REST API services, the PROFINET and PROFIsafe communication services, the DCP configuration functions, and the firmware update service. These interfaces represent the main externally reachable attack surface of the device and therefore form the focus of the security assessment presented in this thesis.

Table \ref{tab:assets} summarises the assets and their protection objectives. It is not exhaustive, but it covers the main elements that are relevant for the security assessment.
{\renewcommand{\arraystretch}{1.2}
	\small 
	\begin{xltabular}{\textwidth}{@{} 
			c 
			>{\raggedright\arraybackslash\hsize=0.9\hsize}X 
			>{\raggedright\arraybackslash\hsize=1.4\hsize}X 
			>{\raggedright\arraybackslash\hsize=0.7\hsize}X 
			@{}}
		
		% --- Kopf- und Fußzeilen-Definition für die Longtable ---
		
		% 1. Kopfzeile auf der ersten Seite (inkl. Caption)
		\caption{Overview of Assets and Protection Objectives}
		\label{tab:assets} \\
		\toprule
		\textbf{ID} & \textbf{Asset} & \textbf{Description} & \textbf{Protection \newline objectives} \\ 
		\midrule
		\endfirsthead
		
		% 2. Kopfzeile auf allen folgenden Seiten
		\caption*{Overview of Assets and Protection Objectives (continued)} \\
		\toprule
		\textbf{ID} & \textbf{Asset} & \textbf{Description} & \textbf{Protection \newline objectives} \\ 
		\midrule
		\endhead
		
		% 3. Fußzeile auf allen Seiten außer der letzten
		\midrule
		\multicolumn{4}{r}{\footnotesize\textit{Continued on next page}} \\
		\endfoot
		
		% 4. Fußzeile auf der allerletzten Seite der Tabelle
		\bottomrule
		\endlastfoot
		
		% --- Eigentlicher Tabelleninhalt ---
		
		A & Trusted Safety Function & SRIO correctly executes its specified safety function. & Integrity, \newline Availability \\ 
		
		B & Integrity of Safety Configuration & Safe behavior is determined by the intended configuration. & Accountability, \newline Authorization, \newline Integrity, \newline Authenticity \\ 
		
		C & Integrity and Authenticity of Safety-Relevant Process Data & Input, output and PROFIsafe data correspond to the actual safety state. & Integrity, \newline Authenticity, \newline Availability \\ 
		
		D & Authenticity and Integrity of Safety Software & Bootloader and firmware remain authentic and unmodified. & Accountability, \newline Authenticity, \newline Integrity \\ 
		
		E & Integrity of Safety Monitoring & Self-tests, plausibility checks, diagnostics and fault responses remain trustworthy. & Integrity, \newline Availability \\ 
		
		F & Separation of Safety and Non-Safety Domain & A compromised COM system must not affect the integrity of the safety function. & Confidentiality, \newline Integrity, \newline Availability \\ 
		
		G & Integrity of Operating Mode & Test and update functions must not be activated or used without authorization. & Accountability, \newline Authorization, \newline Integrity, \newline Authenticity, \newline Availability \\ 
		
		H & SRIO Functionality & Availability of SRIO functionality shall be ensured. & Availability \\ 
		
		I & Integrity, Availability and Confidentiality of Audit/Tracing Data & Evidence of interventions, configuration changes and installed software versions must be generated, retained for the mandated period and access-restricted. & Accountability, \newline Availability, \newline Confidentiality \\
		
	\end{xltabular}
}

\subsection{Implemented Test Environment}
\label{subsec:realized_environment}
The test environment used one switch and one flat address range. Figure \ref{fig:testbed_topology} shows the physical topology, and Table \ref{tab:testbed_components} lists the components and their roles.

\begin{table}[H]
	\centering
	\small
	\begin{tabularx}{\textwidth}{l l c X}
		\toprule
		\textbf{Component} & \textbf{Address} & \textbf{Port} & \textbf{Role} \\
		\midrule
		Industrial switch & 192.168.0.91 & -- & Layer-2 connectivity for all nodes \\
		PLC (F-Host) & 192.168.0.1 & 1 & Safety controller and PROFINET/PROFIsafe counterpart \\
		Safe Remote I/O (SUT) & 192.168.0.2 & 2 & Device under test \\
		Kali Linux test system & 192.168.0.80 & 3 & Security test system, connected through a USB 3.0 to Gigabit Ethernet adapter \\
		Company laptop & 192.168.0.7 & 4 & Engineering station for TIA Portal and I\&M reads \\
		\bottomrule
	\end{tabularx}
	\caption{Components of the implemented test environment}
	\label{tab:testbed_components}
\end{table}

All nodes shared the 192.168.0.0/24 segment and one Ethernet broadcast domain. No VLAN separation was configured. The test system reached the SUT and the controller directly over IP. The test object was the SRIO on port 2. The test initiator was the Kali system on port 3. The company laptop on port 4 served as the engineering station and read identification and maintenance data through TIA Portal. The available observation points were the direct request and response traffic of the test system and the HTTP responses of the IoT-Core service. Traffic that was not addressed to the test system was not necessarily visible, because a switch forwards unicast frames only to the destination port. NV1 was therefore the realised position. NV2 and NV3 were not implemented.

The device documentation defines organisational preconditions for secure operation. The intended use assumes the device in a protected zone behind a firewall. A firmware update is expected to be performed by a trusted person behind that firewall. A security risk assessment is required. The debug interface is primarily a physical interface. These preconditions bound the threat model of the practical tests.


\begin{figure}[H]
	\centering
	\begin{tikzpicture}[
		% Definition der Design-Stile für die Bachelorarbeit
		node distance=1cm and 1cm,
		box/.style={
			draw=black!70, 
			thick, 
			rectangle, 
			rounded corners=1.5mm, 
			minimum width=4.2cm, 
			minimum height=1.8cm, 
			align=center, 
			drop shadow={opacity=0.15}, 
			fill=white,
			font=\small
		},
		switch/.style={
			box, 
			fill=gray!10, 
			minimum height=2cm, 
			minimum width=4.5cm
		},
		link/.style={
			draw=black!80, 
			thick, 
			<->, % Bidirektionale Netzwerkverbindung
			>=Latex
		},
		port/.style={
			fill=white, 
			inner sep=2pt, 
			font=\scriptsize\bfseries,
			text=black!80
		}
		]
		
		% Zentrales Element: Der Switch
		\node[switch] (switch) {
			\textbf{Industrial Switch}\\
			192.168.0.91\\[1.5mm]
			\textit{\scriptsize Layer-2 connectivity}
		};
		
		% Periphere Knoten (Nodes)
		\node[box, above left=of switch] (plc) {
			\textbf{PLC (F-Host)}\\
			192.168.0.1\\[1.5mm]
			\textit{\scriptsize Safety controller}
		};
		
		\node[box, above right=of switch] (sut) {
			\textbf{Safe Remote I/O (SUT)}\\
			192.168.0.2\\[1.5mm]
			\textit{\scriptsize Device under test}
		};
		
		\node[box, below left=of switch] (kali) {
			\textbf{Kali Linux Test System}\\
			192.168.0.80\\[1.5mm]
			\textit{\scriptsize Security test system}
		};
		
		\node[box, below right=of switch] (laptop) {
			\textbf{Company Laptop}\\
			192.168.0.7\\[1.5mm]
			\textit{\scriptsize Engineering station}
		};
		
		% Verbindungen (Kabel) ziehen und Ports am Switch beschriften
		% pos=0.75 platziert das Port-Label nahe am Switch
		\draw[link] (plc.south east) -- (switch.north west) 
		node[pos=0.75, port, xshift=-0.1cm, yshift=0.2cm] {Port 1};
		
		\draw[link] (sut.south west) -- (switch.north east) 
		node[pos=0.75, port, xshift=0.1cm, yshift=0.2cm] {Port 2};
		
		\draw[link] (kali.north east) -- (switch.south west) 
		node[pos=0.75, port, xshift=-0.1cm, yshift=-0.2cm] {Port 3};
		
		\draw[link] (laptop.north west) -- (switch.south east) 
		node[pos=0.75, port, xshift=0.1cm, yshift=-0.2cm] {Port 4};
		
	\end{tikzpicture}
	\caption{Logical topology and addressing of the implemented test environment}
	\label{fig:testbed_topology}
\end{figure}




\section{Application of the Methodology}
\label{sec:instantiation}
The applicability assessment defined in Section \ref{subsec:company_framework} was applied before each test was selected. The assessment considered the target interface, the required network position, the tool compatibility, and the available observation method. Each requirement was linked to a component requirement, a target asset, and one or more test cases. Each practical activity is described once in Section \ref{sec:executed_tests} and is referenced from both the company identifier and the thesis-specific identifier.

\subsection{Applicability Assessment and Company Framework}
\label{subsec:cm_applicability}
Table \ref{tab:cm_applicability} records the applicability outcome for the ten company tests. The not-applicable outcomes are properties of the device or of the topology, not test failures. Test CM-002 requires a second network zone, which the flat segment does not provide. Test CM-003 requires an authentication endpoint, test CM-005 requires a TLS endpoint, and test CM-006 requires an operating-system shell. The SUT exposes none of these. The absence of authentication and of transport encryption is not discarded. It is assessed separately as an observation in Section \ref{sec:executed_tests}. %TODO überprüfen, ob das so richtig? warum wird es nicht ignoriert?

\begin{table}[htbp]
	\centering
	\small
	\begin{tabularx}{\textwidth}{l l X}
		\toprule
		\textbf{CM} & \textbf{Tool} & \textbf{Applicability outcome} \\
		\midrule
		CM-001 & Nmap & Applicable; performed \\
		CM-002 & Nmap & Not applicable to the topology; no zone or conduit model in a flat segment \\
		CM-003 & Hydra & Not applicable to the device; no authentication endpoint \\
		CM-004 & OWASP ZAP & Applicable with adaptation; web scan performed, multi-role comparison not applicable \\
		CM-005 & SSLscan & Not applicable to the device; no TLS endpoint \\
		CM-006 & Lynis & Not applicable to the device; no operating-system shell \\
		CM-007 & Nikto & Applicable; performed \\
		CM-008 & OpenVAS/GVM & Blocked; scanner could not be brought into operation in the laboratory setup \\
		CM-009 & Siege & Applicable; performed, load summary and degraded availability recorded \\
		CM-010 & boofuzz & Applicable; performed \\
		\bottomrule
	\end{tabularx}
	\caption{Applicability of the company framework tests to the SUT}
	\label{tab:cm_applicability}
\end{table}

\subsection{Mapping of Company Tests and Requirements}
\label{subsec:cm_tc_map}
%TODO überprüfen
Table \ref{tab:cm_tc_map} maps the company tests to the derived requirements and to the security category in which they are reported. One practical activity can support several requirements. The mapping is not one-to-one. For example, one Nmap run supports the exposure countermeasure CM-001 and the identification and asset-discovery category, and one query of the identification interface supports several identification requirements. The tests CM-002, CM-006, and CM-008 do not appear in the mapping, because they are not applicable or could not be operated on this device and therefore support no requirement. The reasons are recorded in Section \ref{subsec:res_na_blocked}.
\begin{table}[H]
	\centering
	\small
	\begin{tabularx}{\textwidth}{l l X X}
		\toprule
		\textbf{CM} & \textbf{Req.} & \textbf{Test category} & \textbf{Evidence} \\
		\midrule
		CM-001, CM-007 & RQ-001 & Identification and asset discovery & Nmap, Nikto \\
		- & RQ-004, RQ-006, RQ-007, RQ-013 & Identification and asset discovery & IoT-Core inventory \\
		CM-005 & RQ-004, RQ-014 & Communication security & SSLscan \\
		- & RQ-003, RQ-008, RQ-012 & Logging, audit and traceability & error log, systick \\
		CM-009 & RQ-007, RQ-010 & Availability and resilience & Siege \\
		CM-010 & RQ-010 & Fuzzing and robustness & boofuzz \\
		CM-003, CM-004 & RQ-011, RQ-014 & Authorisation and access control & ZAP, IoT-Core \\
		\bottomrule
	\end{tabularx}
	\caption{Mapping of company tests, requirements, and security categories}
	\label{tab:cm_tc_map}
\end{table}

\section{Test Execution by Security Category}
\label{sec:executed_tests}
%TODO schreiben in den Einleitungstext, dass jeder Test der durchgührt wird in irgendeiner Form gespeichet werden muss, je nach Test als z.B. .txt, .html, .log oder screenshot
The two-level test catalogue derived in Chapter \ref{chap:methodology} defines abstract test scenarios for all fourteen requirements, including the tests that depend on the passive-observation variant NV2 and the in-path variant NV3. Only a subset of these was executed in the practical phase: the applicable company tests and the requirement-oriented tests that the NV1 position supports. This section groups the tests into security categories and describes them at the level of the test activity, the method, and the result, rather than by individual test-case identifier. Each category summarises the abstract scenarios it contains, and then reports the concrete execution and the stored evidence for the activities performed under NV1. The tests that require NV2 or NV3 are described as designed but not executed. Every reported result is limited to what the stored evidence shows. A tool execution is not treated as requirement verification, and an expected outcome from a test specification is not presented as an observed result.

Each activity follows the same logic. It states what the requirement expects, what must therefore be checked, how the device implements the property, and how the check was performed under NV1.

\subsection{Identification and Asset Discovery Tests}
\label{subsec:cat_identification}

This category covers the discovery of the reachable attack surface and the identification of the installed software and data, addressing RQ-001 on the non-safety path and requirements RQ-004, RQ-006, RQ-007, and RQ-013. It targets the separation of the safety and non-safety domains and the software and data-identification assets. The abstract catalogue contains the attack-surface enumeration, the identification-surface inventory, the software-bill-of-materials completeness analysis, the plaintext-transport observation, the data-signature uniqueness check, and the version-history and per-upload-timestamp checks. The bill-of-materials, signature-uniqueness, and version-history scenarios were designed but not executed. The activities executed under NV1 are reported below.

\paragraph{Attack-Surface Enumeration}
\label{subsec:res_attack_surface}
This activity examined the externally reachable services on the non-safety path. It relates to CM-001 and CM-007, to requirement RQ-001, and to asset F, the separation of the safety and the non-safety domain. It targets the IoT-Core HTTP path and was run under NV1.

A full TCP port scan and a top-ports UDP scan were run against the SUT with Nmap. The TCP scan covered all 65535 ports and reported one open port, port 80, which returned HTTP responses without a server banner. All other TCP ports were reported as closed. The open HTTP port is consistent with the IoT-Core diagnostic interface. The UDP scan of the top 200 ports reported port 161 as open, identified as SNMP, and port 49152 as open or filtered. An open SNMP port is consistent with a PROFINET device, because the PROFINET standard uses SNMP for network diagnostics. The reported open-port count depends on the scan timing. At the slower \texttt{-T3} timing the scan reported the stable result above, whereas the faster \texttt{-T5} timing reported further open ports. The additional ports did not reappear on repetition, which indicates that the faster timing overloads the device and produces scan artefacts rather than additional services. 

The externally reachable services on the non-safety path are the HTTP service on port 80 and the SNMP service on port 161. Nikto targets the exposure of debug and service interfaces on a web server, which is the objective of the company countermeasure CM-007. On this device the physical debug interface is encapsulated and not reachable, so the scan characterises only the reachable HTTP service surface. The Nikto report lists missing HTTP security headers, including content-security-policy, x-content-type-options, strict-transport-security, and permissions-policy. These headers are a defence-in-depth measure, so their absence is an observation rather than a direct vulnerability. The report also flags several \texttt{/actuator/*} paths as Spring Boot Actuator endpoints that return JSON.

The Nikto findings are unvalidated scanner results \cite[p. 39]{richier2011security}. The Spring Boot Actuator finding is a probable false positive. The device is an ifm IoT-Core and not a Spring Boot application, and its HTTP service returns a generic JSON structure for many paths, which a generic scanner signature can misclassify as an exposed Actuator endpoint. The finding therefore does not, on its own, indicate a risk. It has to be confirmed by requesting one such path directly and inspecting the raw response. %TODO überprüfen, ob das stimmt

The enumeration supports RQ-001 at the level of the non-safety path. It shows which services a connecting device can reach. It does not, on its own, demonstrate a hazardous situation. The SNMP service on port 161 was not examined further.


\paragraph{Identification-Surface Inventory}
\label{subsec:res_identification}
This activity enumerated the identification data that the device exposes. It relates to requirements RQ-004, RQ-006, RQ-007, and RQ-013, and to assets B and D. It targets the IoT-Core \texttt{deviceinfo} and \texttt{firmware} paths and was run under NV1.

The identification interface was queried over HTTP. The interface required no credentials, so the read is anonymous. For each queried path the device returned a JSON response with status code 200 that contained the requested identifier. The reads covered the hardware version and revision, the software revision, the COM and SCPU firmware and bootloader versions, the host firmware version, and the fieldbus firmware version. The successful read is recognisable by the returned status code 200, which indicates a successful response. A separate single read of the software revision also returned status 200, which documents the baseline availability of the interface.

The device exposes top-level version identifiers for the COM firmware, the SCPU firmware, the bootloaders, and the fieldbus firmware, all through anonymous read access. The inventory supports RQ-004, RQ-006, and RQ-007 at product granularity. It confirms that safety-relevant software is identifiable and available on request. It does not establish a complete software bill of materials. The versions of third-party components, such as the communication stack or the real-time operating system, are not individually exposed. The visible data is an identification surface, not a complete installed-software inventory.
%  curl -s http://$SRIO/$p/getdata
% TODO: RQ-013 (current version only) requires a
%   controlled version condition. The inventory shows the current versions, but
%   downgrade behaviour was not tested. Keep RQ-013 partially assessed.
% TODO: RQ-007 requires availability at all times. The
%   evidence shows availability on request. Record whether the interface is also
%   available in other operating states, or keep RQ-007 partially assessed.

\subsection{Communication Security Tests}
\label{subsec:cat_communication}
This category covers the protection of the communication paths against corruption and disclosure, addressing RQ-001, RQ-002, and the transport aspect of RQ-014. It targets the safety-relevant process data and the audit data. The abstract catalogue contains the transport-confidentiality observation on the non-safety path, the integrity-rejection and in-path manipulation scenarios for the safety telegram, and the watchdog-passivation scenario. The safety-telegram integrity, in-path manipulation, and watchdog scenarios require the NV2 or NV3 position or the engineering tool and were designed but not executed. The transport observation executed under NV1 is reported below.

\paragraph{Cleartext Transmission of the Identification Data}
\label{subsec:res_cleartext}
This activity examined the transport protection of the identification data. It relates to CM-005, to requirements RQ-004 and RQ-014, and to asset I. It targets the IoT-Core HTTP service and was run under NV1.

SSLscan verifies the TLS configuration of a TLS endpoint. It probes for a TLS handshake on a given host and port and reports the enabled protocol versions and the certificate. As an example, the tool was directed at the HTTP service on port 80. The saved output shows that no SSL or TLS protocol version is enabled and that no certificate can be parsed. This confirms that no TLS endpoint is exposed on port 80, so the service is plain HTTP without transport encryption. The run also demonstrates that the check works, because the tool correctly reports the absence of TLS. The identification data of Section \ref{subsec:res_identification} is served over this cleartext channel.
%sslscan <Target-IP>:<Port> --show-certificate > cm005_<target>.txt
The absence of TLS is a device characteristic, not a failed TLS test. It is relevant to RQ-014 as a defence-in-depth observation. A confidentiality impact requires two further conditions. The transmitted data must be sensitive, and an unauthorised party must be able to observe it. The second condition depends on the NV2 position, which was not realised. The confidentiality claim is therefore limited to the cleartext transmission that the test system observed in its own requests and responses.

\subsection{Logging, Audit and Traceability Tests}
\label{subsec:cat_logging}
This category covers the evidence, accountability, and retention requirements RQ-003, RQ-008, RQ-009, and RQ-012, with the audit-data asset. The abstract catalogue contains the intervention-evidence baseline, the time-attribution and cold-start-deletion scenarios, the software- and configuration-modification evidence scenarios, and the circular-buffer and retention scenarios. The software-installation and configuration-modification evidence scenarios require an induced software or configuration change and were not fully executed. The intervention-evidence and time-attribution activity executed under NV1 is reported below; the log-access probe is reported under the authorisation and access-control category.
\paragraph{Intervention Evidence and Time Attribution}
\label{subsec:res_evidence}
This activity examined what evidence the device records for an intervention and how it is time-referenced. It relates to requirements RQ-003, RQ-008, and RQ-012, and to assets I and G. It targets the IoT-Core \texttt{errorlog} and \texttt{systick} paths and was run under NV1.

The regulation requires that interventions be logged as evidence, and usable evidence needs a time reference. A time reference requires a persistent clock. The device provides only a relative system tick and an error log, and the error log is cleared at a cold restart. The activity therefore checks whether the recorded evidence carries an absolute time and whether it persists across a power cycle.

The error log and the system tick counter were read over HTTP in a controlled sequence. The sequence recorded a baseline, an induced connection loss by short-circuiting pin 1 and pin 3 of a digital input of the SRIO, a cold restart of the device by removing power, and a second induced connection loss. Table \ref{tab:rq003_evidence} lists the recorded system tick values and the number of log entries at each step.

\begin{table}[htbp]
	\centering
	\small
	\begin{tabular}{l r r}
		\toprule
		\textbf{Step} & \textbf{System tick} & \textbf{Log entries} \\
		\midrule
		Baseline & 18329 & 0 \\
		After event 1 (cable pull) & 44979 & 2 \\
		After cold restart & 27509 & 0 \\
		After event 2 (cable pull) & 49889 & 1 \\
		\bottomrule
	\end{tabular}
	\caption{Recorded system tick values and error-log entry counts}
	\label{tab:rq003_evidence}
\end{table}
%TODO: Verify the four system tick values in Table \ref{tab:rq003_evidence} against the stored systick files (cm_rq003-04_systick_00 to 03). The baseline value in the stored file differs from the value shown in the table.

At the baseline the error log was empty. The short-circuit at the digital input triggered a fault, which produced two error entries on the safety CPU. Each entry carried a relative system tick value but no absolute timestamp and no actor identity. After the cold restart the error log was empty again, and the system tick had dropped, as Table \ref{tab:rq003_evidence} shows. The second connection loss produced a new entry. Because the system tick is reset by a restart, an entry recorded after the restart can carry a lower tick than an earlier entry, although it occurred later in real time.

The device does collect evidence of the connection-loss events, which supports the accessibility part of RQ-003. Three limitations are visible in the evidence. First, the entries carry only a relative system tick, not an absolute timestamp, so the time attribution required by RQ-003 is not met. Second, the system tick resets at a cold restart, so entries cannot be ordered across a power cycle. An entry recorded after the restart can carry a lower tick than an earlier entry, which confirms the reset. Third, the log content did not persist across the cold restart. The two entries from the first event were absent after the restart. This directly demonstrates the cold-start deletion behind RQ-012, so the retention requirement is not met for this log.
% TODO[RESULT][TC-RQ008-01]: The events observed here are connection and SCPU
%   errors. RQ-008 concerns software-installation events. Add evidence from a
%   software or configuration change before assessing RQ-008 beyond the baseline.
%   Required evidence: initial state, install trigger, exported event data.
% TODO[ASSESSMENT][TC-RQ003-04]: The system tick is interpreted as a relative
%   uptime counter. Confirm the unit and the reset behaviour from the device
%   documentation before the final wording.

\subsection{Availability and Resilience Tests}
\label{subsec:cat_availability}
This category covers the availability aspect of RQ-007 and the resilience aspect of RQ-010, with the availability and domain-separation assets. The abstract catalogue contains the baseline resilience scenario, the network and layer-two flood scenario, the application-layer flood and domain-separation scenario, the connection-hold self-denial-of-service scenario, and the aggregate outcome-verification scenario. The application-layer load test, the connection-hold procedure, and the layer-two flood were executed under NV1. The domain-separation confirmation depends on observation of the safety channel and was designed but not executed, because it requires the NV2 position. The activities executed under NV1 are reported below; the protocol-fuzzing activity is reported under the fuzzing and robustness category.
\paragraph{Availability under Connection Load}
\label{subsec:res_availability}
This activity examined the availability of the identification interface under connection load. It relates to CM-009, to requirements RQ-007 and RQ-010, and to assets H and F. It targets the IoT-Core HTTP service and was run under NV1.

A load test with Siege was run against the IoT-Core HTTP service at 50 and at 3 concurrent connections. The two load levels reflect the framework default and the device-specific expectation for the SRIO, whose requirement documentation permits at most two concurrent connections. The console output and the Siege summary logs are stored as evidence.

The stored summaries show that the service responded to a large number of requests with HTTP status 200 and that a substantial number of connections failed in the same runs. Under the 50-connection load, individual runs recorded between 53 and 361 successful transactions and between 65 and 100 failed connections, and in one run no transaction completed over an interval of about 27 seconds. The interface therefore remained partly responsive under load, but its availability was degraded and a share of the connections was refused.

A connection-hold procedure was also executed against the identification interface. The procedure opened the available connection slots of the HTTP server and then issued a further request, which was expected to time out while the slots were held. The stored evidence shows that this additional request still returned HTTP status 200 within about 0.25 seconds while the slots were held. The expected self-denial-of-service therefore did not occur, and the interface remained available.
%TODO

The activity documents a degraded but non-zero availability under application-layer load, which supports the availability aspect of RQ-007 and the application-layer aspect of RQ-010. A statement about domain separation or about the safety function is not possible under NV1, because the safety channel was not observed.
%TODO: If a precise figure is required, extract the aggregate availability and failure rates from cmrq010_siege.log and cmrq010_3connections_siege.log and report them here. Confirm which rows correspond to the 50-connection and the 3-connection runs.

\paragraph{Layer-Two and Network Flooding}
\label{subsec:res_l2flood}
This activity examined the resilience of the device against a link-layer and network flood. It relates to requirement RQ-010 and to assets H and F. It targets the shared Ethernet segment and was run under NV1.

A packet flood was generated from the test system with hping3 in flood mode, and a MAC-address flood was generated with macof. The stored terminal output records that the hping3 run transmitted more than twelve million packets and received no replies. No device fault was observed in the indicators available to the test system. The effect on the cyclic safety channel could not be observed under NV1, because that traffic is not visible to the test system. %TODO müsste ich es also eigentlich in einer anderen Testumgebung wie NV2 oder NV3 durchführen?

The flood was executed, and no device fault was observed in the available indicators. The result does not, on its own, demonstrate resilience of the safety function, because the safety channel was not monitored. Requirement RQ-010 therefore remains inconclusive for the hazardous-situation clause at the link layer.
%TODO: State exactly which device indicators were observed during the flood (for example status LEDs or the TIA diagnostics view) so that "no device fault was observed" is precisely scoped.

\subsection{Fuzzing and Robustness Tests}
\label{subsec:cat_robustness}
This category covers the robustness aspect of RQ-010 under malformed input, with the availability asset. The abstract catalogue contains the protocol-fuzzing scenario for the fieldbus-discovery, safety-frame, and web-interface inputs. The fieldbus-discovery and safety-frame fuzzing require the NV2 or NV3 position and were designed but not executed. The web-interface fuzzing executed under NV1 is reported below.

\paragraph{Robustness under Malformed Input}
\label{subsec:res_robustness}
This activity examined the robustness of the HTTP service against malformed input. It relates to CM-010, to requirement RQ-010, and to asset H. It targets the IoT-Core HTTP service and was run under NV1.

A fuzzing test was run with boofuzz against the HTTP service on port 80. The saved script fuzzes the request line of an HTTP GET request. The boofuzz session databases and the run log are stored as evidence. The log records that most malformed requests were processed without an observable effect, but that after one malformed request the HTTP service became unreachable. Boofuzz could not re-open the connection for about forty seconds, and its monitor attributed the failure to the preceding test case, after which the service recovered. During the run the device signalled a bus fault and a system fault on its status LEDs, and an error with code 0x1000 was observed on the device. These are device-side observations and are not recorded in the boofuzz log.

The stored evidence therefore shows a temporary disruption of the HTTP service on the non-safety path under malformed input, followed by an automatic recovery. The disruption is a finding that requires follow-up by development. The causation to a specific input, the reproducibility, and any effect on the safety function are not established, because the safety channel was not monitored. Requirement RQ-010 therefore remains inconclusive for the hazardous-situation clause, while the robustness weakness of the HTTP interface is now evidenced. The result demonstrates the value of robustness testing, because a structured fuzzing run surfaced a device-side disruption that would otherwise remain undetected.

\subsection{Authorisation and Access Control Tests}
\label{subsec:cat_access}
This category covers the modification-prevention and access-restriction requirements RQ-011 and RQ-014, together with the configuration-modification evidence of RQ-009, with the operating-mode, configuration, and audit-data assets. The abstract catalogue contains the read-only enforcement scenario, the unauthenticated configuration-protocol reset, the fieldbus parameter-write scenario, the firmware-update mode gate, the physical-barrier scenario, the unauthenticated tracing-log read, and the access-control and deletion-control probes. The device exposes a single anonymous read-only role and no authentication endpoint, so the authentication-oriented scenarios reduce to the observation that no identity model exists. The configuration-protocol and physical-barrier scenarios were designed but not executed. The read-only write probe and the access-control probe executed under NV1 are reported below.
\paragraph{Access Control and Configuration Interfaces}
\label{subsec:res_access_control}
This activity examined the access-control model of the non-safety interface. It relates to CM-003 and CM-004, to requirements RQ-011, RQ-014, and RQ-009, and to assets B and G. It targets the IoT-Core interface and the configuration protocol and was run under NV1.

The IoT-Core interface provided anonymous read access without a login. No authentication endpoint was present, so the Hydra procedure of CM-003 was not applicable. An active web scan was run with OWASP ZAP through a local proxy, and the report was saved. A comparison of multiple user roles was not applicable, because the interface exposes a single anonymous read-only role.

A write probe was run against three IoT-Core nodes. A POST request to the safety-address node, to a digital-output node, and to the application-tag node each returned the response code 400. The write attempts were therefore rejected on the three examined nodes, and the responses are stored as evidence.
%TODO: The application-tag node is documented as writable, yet the POST also returned code 400. Confirm from 209_SRIO_CompSpec_Module_Com 5.3.1 whether the 400 reflects the request format or an access-control decision, so the read-only conclusion is not overstated.
% TODO[RESULT][TC-RQ011-02]: The unauthenticated factory-reset test is not
%   evidenced. Do not infer a reset from discovering a command or a protocol.
%   Record whether the reset command was sent, accepted without authentication,
%   whether the device state changed, and whether the device was restored.
% TODO[RESULT][TC-RQ009-01]: Configuration-modification evidence requires an
%   attempted or a successful configuration change with before and after states.
% TODO[ASSESSMENT][TC-RQ014-03]: Do not interpret anonymous read access to the
%   IoT-Core as proof that the whole device has no access control. Name the exact
%   interface and mechanism that was probed.

The anonymous read-only access is an observation about the IoT-Core interface. It does not, on its own, decide RQ-011 or RQ-014. The write probe gives partial evidence for the read-only enforcement of RQ-011 on the three examined nodes. It does not cover the full write surface, the configuration-protocol behaviour, or the factory-reset path, which were not exercised. The tracing-log access control of RQ-014 is not evidenced beyond the single anonymous read-only role. These requirements therefore remain partially assessed.

\subsection{Firmware and Configuration Integrity Tests}
\label{subsec:cat_config_firmware}
This category covers the integrity and authenticity of safety-relevant software and data under RQ-005, the parameter-integrity aspect of RQ-002, and the physical tamper resistance of the address and mode interface. It targets the configuration, firmware, and operating-mode assets. The abstract catalogue contains the parameter-integrity rejection and forgery scenarios, the unsigned and modified firmware installation scenario, the update-package static-analysis scenario, the analytical safety-self-test scenario, and the physical tamper-resistance scenario. These scenarios require the engineering tool, a spare unit for a risk-bearing firmware test, the NV3 in-path position, or physical access to the sealed interface. They were designed but not executed in the practical phase, so no requirement in this category is assessed from stored device evidence. The parameter-integrity, firmware-authenticity, and physical-tamper gaps identified during the theoretical validation are documented as open items for a future test campaign.

\subsection{Not Applicable and Blocked Company Tests}
\label{subsec:res_na_blocked}
Three company tests could not be applied to the device, or blocked by the environment. CM-002 requires a second zone and a conduit model, which the flat laboratory segment does not provide. CM-006 requires an operating-system shell, which the embedded device does not expose. CM-008 could not be operated, because the GVM/OpenVAS scanner could not be brought into a working state within the laboratory setup and the available time. These outcomes are recorded as applicability or environment findings. They are meaningful methodological results, because they show why an applicability assessment is required before execution. They do not indicate a product defect, and they do not support a positive or a negative requirement verdict.

\section{Requirement Verification}
\label{sec:requirement_verification}
%TODO
This section relates the executed activities to the derived requirements. It uses the verdict scale of Section \ref{subsec:verdicts}. All activities were run under the network variant NV1. A verdict is assigned only where a stored artifact supports it. Where the evidence documents a device gap rather than a failed test, the requirement is verdicted FAIL by deviation, because the device does not meet the obligation while the test itself yields a definitive result.

Table \ref{tab:req_verification} summarises the current status per requirement. No requirement is verified as fully met. No legal conformity is claimed. A PARTIAL or an inconclusive assessment reflects the bounded scope of the practical phase.

\begin{table}[htbp]
	\centering
	\small
	\begin{tabularx}{\textwidth}{l l l X}
		\toprule
		\textbf{Req.} & \textbf{Supporting activity} & \textbf{Assessment} & \textbf{Limitation} \\
		\midrule
		RQ-001 & attack-surface enumeration & PARTIAL & non-safety path only; scanner findings unvalidated \\
		RQ-002 & none executed & not assessed & requires NV2, NV3, or the engineering tool \\
		RQ-003 & intervention evidence and time attribution & FAIL (deviation) & no absolute timestamp, no actor identity \\
		RQ-004 & identification inventory; cleartext transport & PARTIAL & no complete software bill of materials \\
		RQ-005 & none executed & not assessed & firmware and integrity tests not executed \\
		RQ-006 & installed-software inventory & PARTIAL & product granularity only \\
		RQ-007 & identification availability; load test & PARTIAL & availability on request; degraded under load \\
		RQ-008 & software-event evidence baseline & inconclusive & no software-installation event induced \\
		RQ-009 & none executed & not assessed & no configuration change induced \\
		RQ-010 & HTTP fuzzing; load test; layer-two flood & inconclusive & hazardous-situation clause needs the safety channel \\
		RQ-011 & write probe on three nodes & PARTIAL & three nodes only; full write surface open \\
		RQ-012 & cold-start deletion & FAIL (deviation) & log cleared by a cold restart \\
		RQ-013 & current-version confirmation & PARTIAL & downgrade behaviour not tested \\
		RQ-014 & cleartext transport; access-control probe & PARTIAL & single anonymous role; exposure needs NV2 \\
		\bottomrule
	\end{tabularx}
	\caption{Current requirement verification status under NV1}
	\label{tab:req_verification}
\end{table}

The strongest evidence concerns the intervention log. The device records connection-loss events, but the entries carry only a relative system tick, and the log content is cleared by a cold restart. The evidence therefore supports the conclusion that the device deviates from the time-attribution obligation behind RQ-003 and from the retention obligation behind RQ-012. The identification tests support RQ-004, RQ-006, and RQ-007 at product granularity, but not at component granularity, because no software bill of materials is exposed. The robustness test and the load test leave RQ-010 inconclusive for the hazardous-situation clause, because the safety channel was not observed. The write probe gives partial evidence for RQ-011 on three nodes. The remaining requirements are not assessed, because the corresponding configuration, firmware, and safety-channel evidence is not available under NV1.
%TODO: Confirm the per-requirement assessments in Table \ref{tab:req_verification} once the outstanding evidence (configuration change, firmware, NV2 capture) is available. RQ-002 and RQ-005 have no device evidence in the current phase.

% ===== Legacy draft of the requirement-verification section (kept disabled) =====
%Table \ref{tab:status_matrix} summarizes the status of the test activities. It uses the status dimensions of Section \ref{subsec:evidence_status}. All activities were run under network variant NV1. The value ``supported (deviation)'' means that the evidence supports the test conclusion, and that the conclusion is a documented gap in the device, not a failed test execution.
%
%\footnotesize
%\begin{longtable}{@{}l l l l l l@{}}
%	\caption{Status of the test activities, all under NV1}
%	\label{tab:status_matrix} \\
%	\toprule
%	\textbf{Activity} & \textbf{Req.} & \textbf{Applicability} & \textbf{Execution} & \textbf{Evidence} & \textbf{Assessment} \\
%	\midrule
%	\endfirsthead
%	\toprule
%	\textbf{Activity} & \textbf{Req.} & \textbf{Applicability} & \textbf{Execution} & \textbf{Evidence} & \textbf{Assessment} \\
%	\midrule
%	\endhead
%	\bottomrule
%	\endfoot
%	Attack-surface enumeration & RQ-001 & applicable & performed & raw & partially supported \\
%	Identification inventory & RQ-004 & applicable & performed & raw & partially supported \\
%	Installed-software inventory & RQ-006 & applicable & performed & raw & partially supported \\
%	TLS transport check & RQ-004 & applicable & performed & raw & partially supported \\
%	Identification availability baseline & RQ-007 & applicable & performed & raw & partially supported \\
%	Current-version confirmation & RQ-013 & applicable & partially performed & raw & partially supported \\
%	Intervention-evidence baseline & RQ-003 & applicable & performed & raw & partially supported \\
%	Time-attribution sequence & RQ-003 & applicable & performed & raw & supported (deviation) \\
%	Cold-start deletion & RQ-012 & applicable & performed & raw & supported (deviation) \\
%	Software-event evidence baseline & RQ-008 & applicable & partially performed & raw & not yet assessed \\
%	HTTP fuzzing robustness & RQ-010 & applicable & performed & raw & inconclusive \\
%	Connection-hold self-DoS & RQ-007 & applicable & status unclear & none & not yet assessed \\
%	Layer-two flood resilience & RQ-010 & partially applicable & not yet performed & none & not yet assessed \\
%	Application-layer flood & RQ-010 & partially applicable & partially performed & none & not yet assessed \\
%	Read-only enforcement & RQ-011 & applicable & not yet performed & none & not yet assessed \\
%	Configuration-protocol reset & RQ-011 & applicable & status unclear & none & not yet assessed \\
%	Cleartext log transport & RQ-014 & partially applicable & partially performed & raw & partially supported \\
%	Access-control probe & RQ-014 & applicable & partially performed & report & not yet assessed \\
%	Configuration-modification evidence & RQ-009 & applicable & not yet performed & none & not yet assessed \\
%\end{longtable}
%\normalsize
%%TODO schlechte darstellung, ist sie notwendig?
%Table \ref{tab:req_verification} relates the requirements to the supporting activities and to the current assessment. No requirement is verified as fully met. No legal conformity is claimed. A partial or an inconclusive assessment reflects the incomplete state of the practical work.
%
%\begin{table}[htbp]
%	\centering
%	\small
%	\begin{tabularx}{\textwidth}{l l l X}
	%		\toprule
	%		\textbf{Req.} & \textbf{Supporting activity} & \textbf{Assessment} & \textbf{Limitation} \\
	%		\midrule
	%		RQ-001 & attack-surface enumeration & partially supported & non-safety path only; scanner findings unvalidated \\
	%		RQ-003 & intervention evidence and time attribution & partially supported & no absolute time, no actor identity \\
	%		RQ-004 & identification inventory & partially supported & no complete software bill of materials \\
	%		RQ-006 & installed-software inventory & partially supported & product granularity only \\
	%		RQ-007 & identification availability & partially supported & availability on request; other states untested \\
	%		RQ-008 & software-event evidence & not yet assessed & software-installation event evidence missing \\
	%		RQ-010 & HTTP fuzzing robustness & inconclusive & error 0x1000 unresolved; load evidence missing \\
	%		RQ-012 & cold-start deletion & partially supported & cold-start deletion observed \\
	%		RQ-013 & current-version confirmation & partially supported & downgrade not tested \\
	%		RQ-014 & cleartext transport & partially supported & cleartext observed; exposure needs NV2 \\
	%		\bottomrule
	%	\end{tabularx}
%	\caption{Current requirement verification status}
%	\label{tab:req_verification}
%\end{table}
%
%The strongest evidence concerns the intervention log. The device records connection-loss events, but the entries carry only a relative system tick, and the log content is cleared by a cold restart. The evidence therefore supports the conclusion that the device deviates from the time-attribution obligation behind RQ-003 and from the retention obligation behind RQ-012. The identification tests support RQ-004, RQ-006, and RQ-007 at product granularity, but not at component granularity, because no software bill of materials is exposed. The robustness test leaves RQ-010 inconclusive, because the observed error 0x1000 is not yet explained. The remaining requirements are not yet assessed, because the corresponding write, configuration, and load evidence is not stored.
%%TODO RQ-002 und RQ-005 fehlen
\subsection{Application of the Evaluation Metrics}
\label{subsec:metrics_application}
The indicators of Section \ref{sec:evaluation_metrics} are applied to the executed subset. The counts follow the status of Table \ref{tab:req_verification} and the activity reports of Section \ref{sec:executed_tests}. They are reported over the performed subset, and the number of cases is stated alongside each ratio.

Fifteen test activities were at least partially executed under NV1. Two of them produced a definitive requirement outcome, both a FAIL by deviation, on the time-attribution and the cold-start-deletion activities. The remaining activities produced partial or inconclusive outcomes. Almost all executed activities are backed by stored raw artifacts, including the Nmap and Nikto output, the SSLscan output, the inventory reads, the error-log and system-tick reads, the Siege summaries, the connection-hold output, the layer-two flood output, the write-probe responses, the boofuzz session databases, and the OWASP ZAP report.

The Requirement Compliance Rate is $RCR = 0$ over the applicable requirements, because no requirement reached a full PASS verdict in the bounded practical phase. This value records the absence of a full-conformance verdict, not a claim that the device is wholly non-conformant. The Test Effectiveness Ratio is $TER = 2/15 \approx 0.13$, because two of the fifteen executed activities produced a definitive PASS or FAIL. The low value reflects that most activities produced a partial or an inconclusive outcome rather than a testbed error, and the partial cases are expected to become definitive once the outstanding evidence is added. The Evidence Traceability Score is high, because almost every executed activity is supported by stored raw output. The Threat Mitigation Coverage is not computed here, because the modelled-threat baseline is maintained per test case in the catalogue and was not aggregated into a single denominator; its computation is deferred to a completed test campaign.
%TODO: Confirm the exact counts for TER and ETS once the final list of executed activities is fixed. The count of fifteen activities follows the activity reports in Section \ref{sec:executed_tests}.
%TODO anpassen auf neue/andere Formeln

These indicators confirm the character of the work. The methodology is applied and traceable, and the evidence base is largely raw and reproducible. The low compliance and effectiveness ratios reflect the deliberately bounded scope of the practical phase rather than a deficiency of the method.

\section{Validation against the Thesis Objective}
\label{sec:objective_validation}
This section validates the developed methodology against the thesis objective. The evaluation follows the evaluation activity of the design science research process \cite[p. 46]{Peffers}. The thesis objective is to develop and validate a systematic methodology that supports the verification of security functions for industrial gateways at the physical IT/OT interface.

The methodology was applied end to end on a real device. It derived requirements from the regulation, connected them to assets and interfaces, identified applicable and non-applicable tests before execution, adapted the generic company procedures, collected evidence, and related observations to technical requirements. Table \ref{tab:goal_validation} maps each objective component to the methodology element that realises it, to the practical evidence, and to the validation boundary.

\begin{table}[htbp]
	\centering
	\small
	\begin{tabularx}{\textwidth}{X X l}
		\toprule
		\textbf{Objective component} & \textbf{Methodology element and evidence} & \textbf{Status} \\
		\midrule
		Derive requirements from the regulation & MVO to prEN 50742 to IEC 62443-4-2 derivation; 14 requirements (Appendix \ref{app:requirement_derivation}) & demonstrated \\
		Connect requirements to assets and interfaces & asset mapping and asset table; requirement-to-asset links & demonstrated \\
		Assess security testing approaches & structured comparison and selected combination & demonstrated \\
		Evaluate the company framework & applicability assessment; Table \ref{tab:cm_applicability} & demonstrated \\
		Identify tool-to-target mismatches & CM-003, CM-005, CM-006 not applicable; CM-008 blocked & demonstrated \\
		Define requirement-oriented test cases & two-level catalogue; executed subset & demonstrated in part \\
		Define test-environment variants & NV1, NV2, NV3 model; NV1 realised & demonstrated for NV1 \\
		Apply the method to a real object & SRIO instantiation; stored evidence & demonstrated \\
		Distinguish applicable, blocked, and open tests & status model; Tables \ref{tab:cm_applicability} and \ref{tab:req_verification} & demonstrated \\
		Support integration into future releases & lifecycle and governance model; reusable schema & demonstrated by design \\
		\bottomrule
	\end{tabularx}
	\caption{Validation of the methodology against the thesis objective}
	\label{tab:goal_validation}
\end{table}

The empirical validation covered the central methodology steps under NV1. The requirement derivation, the asset mapping, the applicability assessment, the test selection, the execution, and the evidence-based assessment were all exercised on a real device. The tests that require passive observation or inline manipulation remain extensions of the validated core process. The methodology is therefore validated in part, within a defined boundary at the NV2 and NV3 variants and at the requirements whose evidence is still open. A claim of full validation is not made.

% ===== Legacy draft of the metrics and validation sections (kept disabled) =====
%\label{subsec:metrics_application}
%The verdicts and indicators of Section \ref{sec:evaluation_metrics} are applied to the executed subset. The status values of Table \ref{tab:status_matrix} map onto the verdict scale as follows. A ``partially supported'' outcome corresponds to a PARTIAL verdict. A ``supported (deviation)'' outcome corresponds to a FAIL at requirement level, because the device demonstrably does not meet the obligation, while the test itself yields a definitive result. An ``inconclusive'' outcome and a ``not yet assessed'' outcome are not scored.
%
%Of the nineteen test activities in Table \ref{tab:status_matrix}, fourteen were at least partially executed. Two of these produced a definitive requirement outcome, both a FAIL by deviation, on the time-attribution and cold-start-deletion activities. Eight were assessed as partial, one as inconclusive, and three as not yet assessed. Twelve of the fourteen executed activities are backed by raw digital artifacts, including the stored OWASP ZAP report and the boofuzz session databases; the two Siege load runs were stored but are void.
%
%The indicators follow from these counts. The Requirement Compliance Rate is $RCR = 0/10 = 0$, because no requirement reached a full PASS verdict. The two deviations against RQ-003 and RQ-012 are demonstrated non-conformances, and the remaining requirements are partial or open. The value therefore records the absence of a full-conformance verdict in a time-bounded practical phase, not a claim that the device is wholly non-conformant. The Test Effectiveness Ratio is $TER = 2/14 \approx 0.14$. It is low because most executed tests produced a partial or open outcome rather than a testbed error, and the partial cases are expected to become definitive once the outstanding write, configuration, and load evidence is added. The Evidence Traceability Score is $ETS = 12/14 \approx 0.86$, which reflects that most executed tests are supported by stored raw output. The Threat Mitigation Coverage is not computed here, because the modeled-threat baseline is maintained per test case in the test catalogue and was not aggregated into a single quantified denominator; its computation is deferred to a completed test campaign.
%
%These indicators confirm the character of the work. The methodology is applied and traceable, and the evidence base is largely raw and reproducible. The low compliance and effectiveness ratios reflect the deliberately bounded scope of the practical phase rather than a deficiency of the method.
%
%\section{Validation against the Thesis Objective}
%\label{sec:objective_validation}
%This section validates the developed methodology against the thesis objective. The evaluation follows the evaluation activity of the design science research process \cite[p. 46]{Peffers}. The thesis objective was to develop and validate a systematic methodology that supports the verification of security functions for industrial gateways at the physical IT/OT interface.
%
%The thesis objective was achieved. The developed methodology provides a systematic and traceable process for deriving, selecting, executing, and assessing security tests. Its practical application to the Safe Remote I/O demonstrated that the process can derive requirements from the regulation, connect them to assets and interfaces, identify applicable and non-applicable tests before execution, adapt generic company procedures, collect evidence, and relate observations to technical requirements. Table \ref{tab:goal_validation} maps each objective component to the methodology element that realizes it, to the practical evidence, and to the validation boundary.
%
%\begin{table}[htbp]
%	\centering
%	\small
%	\begin{tabularx}{\textwidth}{X X l}
	%		\toprule
	%		\textbf{Objective component} & \textbf{Methodology element and evidence} & \textbf{Status} \\
	%		\midrule
	%		Derive requirements from the regulation & MVO to prEN 50742 to IEC 62443-4-2 derivation; 14 requirements (Appendix) & validated \\
	%		Connect requirements to assets and interfaces & asset mapping and asset table; requirement-to-asset links & validated \\
	%		Assess security testing approaches & structured comparison and selected combination (methodology) & validated \\
	%		Evaluate the company framework & applicability assessment; Table \ref{tab:cm_applicability} & validated \\
	%		Identify tool-to-target mismatches & CM-003, CM-005, CM-006 not applicable; CM-008 blocked & validated \\
	%		Define requirement-oriented test cases & two-level requirement-oriented catalogue; executed subset & validated in part \\
	%		Define test-environment variants & NV1, NV2, NV3 model; NV1 realized & validated for NV1 \\
	%		Apply the method to a real object & SRIO instantiation; stored evidence & validated \\
	%		Distinguish applicable, blocked, and open tests & status model; Table \ref{tab:status_matrix} & validated \\
	%		Support integration into future releases & lifecycle and governance model; reusable schema & validated by design \\
	%		\bottomrule
	%	\end{tabularx}
%	\caption{Validation of the methodology against the thesis objective}
%	\label{tab:goal_validation}
%\end{table}
%
%The empirical validation covered the central methodology steps under NV1. The requirement derivation, the asset mapping, the applicability assessment, the test selection, the execution, and the evidence-based assessment were all exercised on a real device. Tests that require passive observation or inline manipulation remain extensions of the validated core process. The methodology is therefore validated for its intended purpose, with a defined boundary at the NV2 and NV3 variants and at the requirements whose evidence is still open.

