\chapter{Methodology}
\label{chap:methodology}

Building upon the theoretical foundations established in Chapter \ref{chap:fundamentals}, this chapter develops the security testing methodology used in this thesis. The methodology translates the cybersecurity obligations of Regulation (EU) 2023/1230 into a traceable and repeatable engineering procedure. It targets the physical IT/OT interface of a safety-related industrial gateway.

The chapter follows a methodology development logic. Section \ref{sec:research_design} defines the research design, the initial situation, the constraints, and the scope. Section \ref{sec:traceability} derives the security requirements and establishes the traceability model. Section \ref{sec:approach_selection} evaluates established testing approaches and justifies the selected combination. Section \ref{sec:test_derivation} specifies how individual test cases are derived and documented. Section \ref{sec:test_environment} defines the test environment and the network access variants. Section \ref{sec:execution_governance} describes the execution procedure and the organizational governance. Section \ref{sec:evaluation_metrics} defines how test results are aggregated into requirement verdicts.


\section{Research Design and Methodological Scope}
\label{sec:research_design}

This research follows the Design Science Research Methodology (DSRM) proposed by \citeauthor{Peffers} \cite[p. 46]{Peffers}. DSRM structures the creation and evaluation of an artifact that solves a practical engineering problem. The artifact of this thesis is a standards-based methodology for the functional security testing of an industrial gateway at the IT/OT interface. The six activities of the DSRM process map onto the structure of this thesis:

\begin{enumerate}
	\item \textbf{Problem Identification and Motivation:} The absence of a systematic approach for verifying security functions under Regulation (EU) 2023/1230 is established in Chapter \ref{chap:introduction}.
	\item \textbf{Definition of Objectives:} The objective of a traceable, standards-based verification methodology that uses prEN 50742 and the IEC 62443 series is defined in Chapter \ref{chap:introduction}.
	\item \textbf{Design and Development:} The methodology, its environment, and its governance are designed in this chapter.
	\item \textbf{Demonstration:} The methodology is applied to a physical System Under Test in Chapter \ref{chap:implementation_results_evaluation}.
	\item \textbf{Evaluation:} The results and the residual validation gap are assessed in Chapter \ref{chap:implementation_results_evaluation}.
	\item \textbf{Communication:} The results are documented and discussed in this thesis.
\end{enumerate}

\subsection{Initial Situation and Constraints}
\label{subsec:initial_situation}

The System Under Test (SUT) is a safety-related remote input and output device. It exposes two interfaces with different characteristics. The first interface carries deterministic safety communication over PROFINET and PROFIsafe according to the black-channel principle. The second interface is a diagnostic service reachable over HTTP. The device therefore combines an OT fieldbus path and an IT diagnostic path in a single component.

The company operates an established functional test process. This process does not yet verify security functions in a systematic and requirement-driven manner. The methodology developed here addresses this gap by adding a systematic, requirement-driven security verification. It complements the existing functional and safety validation rather than replacing it.

Several constraints shape the methodology and are treated as fixed influencing factors:

\begin{itemize}
	\item \textbf{Black-box target:} The SUT is evaluated as a compiled product. Its source code is not available, which excludes static source analysis.
	\item \textbf{Isolated laboratory:} Testing is performed in a dedicated laboratory. Production systems are excluded.
	\item \textbf{Single-segment topology:} The laboratory network uses one switch and one flat address range. This limits which network positions are achievable.
	\item \textbf{Safety priority:} The safety function must not be degraded by testing. A cybersecurity measure or a test must never introduce impermissible latency into the safety communication \cite[p. 14]{prEN50742_2025}.
	\item \textbf{Draft normative basis:} prEN 50742 is a draft standard. Its clauses are used as technical guidance, not as a certified conformity baseline.
	\item \textbf{Limited practical time:} The practical test phase is time-limited. This affects the number of executed tests.
\end{itemize}

\subsection{Scope and Delimitation}
\label{subsec:methodological_scope}

The methodology focuses on functional security testing. It verifies whether planned security functions, such as access restrictions or integrity checks, operate as specified \cite[p. 82]{1341418}. Requirement-driven robustness and availability testing is included where a requirement demands it \cite[p. 69]{prEN50742_2025}. This bounded robustness testing is derived from a requirement and differs from open-ended fuzzing that only searches for unknown defects.

Three activities lie outside the scope. Open-ended vulnerability discovery, such as unstructured penetration testing whose goal is to find unknown defects, is excluded. It cannot systematically demonstrate the presence and correct operation of a mandated security function. Static source-code analysis is excluded because no source code is available. Invasive hardware analysis, such as chip-level or side-channel attacks, is excluded because it exceeds a functional verification at the IT/OT interface. The methodology is developed and demonstrated on a single representative SUT.


\section{Traceability}
\label{sec:traceability}
This section derives the security requirements and defines how each requirement remains traceable to a concrete test. The derivation isolates verifiable obligations from legal text. The traceability model then connects each obligation to the evidence that supports its verification.

\subsection{Regulatory Derivation and Asset Mapping}
\label{subsec:derivation_mapping}

The derivation follows three steps. First, precise cybersecurity requirements are extracted from Regulation (EU) 2023/1230. This step isolates specific obligations from general legal text and yields the fourteen requirements documented in Appendix \ref{app:sec:mvo_requirements}. Second, each requirement is mapped to the harmonized standard draft prEN 50742. The regulation defines safety goals, whereas prEN 50742 provides technical guidance for machine design. The resulting mapping is documented in Appendix \ref{app:sec:mapping_pren50742}. Third, each requirement is translated into Component Requirements of IEC 62443-4-2. This step produces granular, component-level specifications for industrial network devices, as documented in Appendix \ref{app:sec:mapping_iec62443}.

A standard clause cannot be tested without a concrete target. The methodology therefore adds a mandatory asset-mapping step. The SUT is decomposed into its physical and logical assets, for example hardware interfaces, communication services, and stored configuration data. Each requirement is paired with the assets to which it applies. This step defines not only what must be verified, but also where the corresponding security function is located.

Threats are identified with STRIDE, which derives threats from potential attacker goals as described in Section \ref{subsec:stride}. The methodology then examines each security function from two directions. The first direction is the threat that the function mitigates. The second direction is the threat to which the function is itself exposed. A transmission checksum, for example, mitigates accidental corruption, yet it remains exposed to intentional forgery when the checksum is not cryptographic. This distinction prevents a function from being credited without an examination of its own weaknesses.

\subsection{Traceability Model}
\label{subsec:traceability_model}
The methodology maintains an unbroken traceability chain (cf. \cite[pp. 36]{DIN_EN_ISO_IEC_15408_1_2024}, \cite[p. 82]{1341418} \cite[p. 21]{DIN_EN_IEC_62443_4_1_2018}). The chain links a legal obligation to the evidence that supports its verification:

\begin{center}
	Requirement $\rightarrow$ Asset $\rightarrow$ Threat $\rightarrow$ Security Function $\rightarrow$ Test Objective $\rightarrow$ Test Technique $\rightarrow$ Access Prerequisite $\rightarrow$ Evidence $\rightarrow$ Result
\end{center}

The nodes of the chain are defined as follows:
\begin{itemize}
	\item \textbf{Requirement:} The regulatory obligation (for example RQ-011) and its mapped IEC 62443-4-2 control (for example CR 2.1).
	\item \textbf{Asset:} The concrete gateway component to which the requirement applies.
	\item \textbf{Threat:} The attacker goal that targets the asset, derived via \hyperref[subsec:stride]{STRIDE}.
	\item \textbf{Security Function:} The implemented countermeasure, together with any identified gap.
	\item \textbf{Test Objective:} The property to be demonstrated for the security function.
	\item \textbf{Test Technique:} The verification type, classified by the IEC 62443-4-1 SVV categories introduced in Section \ref{sec:approach_selection}.
	\item \textbf{Access Prerequisite:} The network position required to run the test, expressed as a network access variant (Section \ref{sec:test_environment}).
	\item \textbf{Evidence:} The recorded artifact, such as a scan output, a packet capture, a log export, or a screenshot.
	\item \textbf{Result:} The observed outcome and its assessment against the acceptance criterion.
\end{itemize}

This chain ensures that every executed test serves a distinct requirement and that every requirement can be traced to concrete evidence.

\section{Selection of the Testing Approach}
\label{sec:approach_selection}
The requirements define what must be verified. This section derives how the verification is performed. It evaluates established approaches, assesses the company testing framework, and justifies the selected combination.

\subsection{Evaluation of Established Approaches}
\label{subsec:approach_evaluation}
Several testing approaches were examined against six criteria: traceability to requirements, availability of concrete test cases, reproducibility, suitability for an embedded OT target, compatibility with functional safety, and evidentiary value.

\begin{itemize}
	\item \textbf{Functional black-box testing} verifies specified behaviour \cite[p. 82]{1341418}. It offers reproducibility and requirement traceability, but it does not assess resistance against misuse.
	\item \textbf{Abstract verification against IEC 62443} defines what must hold at the component level, but it does not provide concrete, executable test cases \cite[p. 37]{DIN_EN_IEC_62443_4_1_2018}. It is a requirement source rather than a test procedure.
	\item \textbf{Unstructured penetration testing} produces realistic attack evidence, but its results depend strongly on the tester and are difficult to reproduce \cite[p. 296]{tauqeer2021analysis}.
	\item \textbf{Fuzzing and robustness testing} suit the evaluation of error handling and availability, but they require a specialised setup and careful result interpretation \cite[p. 29]{richier2011security}.
	\item \textbf{Web and IoT testing guides}, such as the OWASP Web Security Testing Guide, provide procedural depth for HTTP interfaces, but they are not tailored to OT fieldbus communication \cite{manana2026pentest}.
	\item \textbf{Risk-based security testing} prioritises tests by risk, but it requires an extensive prior risk analysis before any test can be executed \cite[p. 37]{doCarmo2025}.
\end{itemize}

No single approach satisfies all criteria. Functional testing and abstract verification provide traceability but no attack perspective. Penetration testing and fuzzing provide an attack perspective but weaker reproducibility. The methodology therefore combines a requirement-driven functional core with bounded robustness testing, and it anchors every test type in a recognised verification taxonomy.

NIST SP 800-115 and MITRE ATT\&CK for ICS are not alternatives in this comparison. NIST SP 800-115 provides the process frame for scoping, execution, and reporting \cite[p. 1-1]{nist_sp800_115}. MITRE ATT\&CK for ICS provides a reference of adversary techniques for the OT domain \cite{mitre_attack_ics}. The methodology adopts both within the selected approach rather than treating them as competing options.

\subsection{ifm Testing Framework and Tool Applicability}
\label{subsec:company_framework}
Ifm provides a security testing framework of ten countermeasure-oriented instructions, denoted CM-001 to CM-010. Each instruction binds one countermeasure to one Kali Linux tool and one acceptance rule. The framework is organised around tools and countermeasures rather than around derived requirements.

The framework cannot be adopted without adaptation. Several instructions assume interfaces that a general IT target exposes but the embedded SUT does not. CM-003 assumes an authentication endpoint, CM-005 assumes a TLS endpoint, and CM-006 assumes an operating-system shell. The SUT provides none of these. This observation motivates an explicit tool-to-target applicability assessment as a step of the methodology. For each planned tool, the methodology checks whether the target exposes the function that the tool assumes.

The applicability assessment produces one of three outcomes. A tool is applicable when the target exposes the assumed interface. A tool is not applicable when the assumed interface is absent, which is an applicability finding and not a product failure. A tool is blocked when an environmental constraint prevents its operation. An absent interface is a property of the device, whereas a blocked tool is a property of the environment. These three outcomes are the minimal distinction that keeps a non-execution from being misread as a failure, because they separate the positive case from the two independent causes of a non-execution.

\subsection{Selected Approach}
\label{subsec:selected_approach}
The selected methodology combines three elements. The first element is a verification taxonomy taken from IEC 62443-4-1 Practice SVV, which distinguishes requirements-based testing, threat-mitigation testing, vulnerability testing, and penetration testing \cite[p. 37]{DIN_EN_IEC_62443_4_1_2018}. The taxonomy anchors every test to a defined verification purpose. The second element is a set of requirement-driven test cases, derived from the fourteen requirements and their asset mapping. The third element is a controlled reuse of the company tools, filtered by the applicability assessment.

This combination satisfies the six criteria. It preserves requirement traceability, it yields concrete and reproducible test cases, and it retains an attack perspective through threat-mitigation and vulnerability tests. It respects the safety priority by bounding the robustness tests to requirement-driven cases. The approach also remains compatible with the existing industrial validation process, because it reuses the company tools where they apply and documents where they do not.

\section{Test Case Derivation and Specification}
\label{sec:test_derivation}
This section defines how a requirement becomes a documented and repeatable test case. It describes the development process, the specification format, the categorisation, and the evidence model.

\subsection{Test Development Process}
\label{subsec:test_development}
Each test case is derived in three steps. The first step is a critical review of the requirement. The implemented security functions and the identified gaps are cross-checked against the device documentation, so that a test verifies a real mechanism rather than an assumed one. The second step is test planning. For each requirement, one or more scenarios are defined. Each scenario states what it proves, its technical justification, and its SVV category. The third step produces the documented test case in a two-level format.

\subsection{Test Specification}
\label{subsec:test_specification}
Every test case is specified on two levels. This separates the product-neutral definition from the environment-specific execution and supports reuse across device variants \cite[p. 37]{Pilorget2012}.

The first level is the abstract test scenario. It is independent of the concrete laboratory and contains the following fields:
\begin{itemize}
	\item \textbf{Traceability:} the requirement, its mapped Component Requirement, and the target asset.
	\item \textbf{Target Threat:} the STRIDE threat that the test exercises. A functional baseline that carries no adversarial goal is marked as not applicable.
	\item \textbf{SVV Category:} the verification type according to IEC 62443-4-1.
	\item \textbf{Objective:} the property to be demonstrated.
	\item \textbf{Steps:} the abstract procedure.
	\item \textbf{Acceptance Criterion:} the exact expected behaviour that constitutes a pass.
\end{itemize}

The second level is the concrete execution. It binds the abstract scenario to the laboratory. It records the required network access variant, the applied tools, the concrete commands, and the result together with its evidence. The procedural depth of the concrete steps follows NIST SP 800-115 for network-level tests, the OWASP Web Security Testing Guide for the HTTP interface, and MITRE ATT\&CK for ICS for adversary techniques in the OT domain \cite[p. 1-1]{nist_sp800_115} \cite{manana2026pentest} \cite{mitre_attack_ics}.

\subsection{Test Categorisation}
\label{subsec:test_categorisation}
Each test case is classified along three dimensions. The first dimension is the SVV category, which states the verification purpose. The second dimension is the network access variant, which states the required position on the communication path (Section \ref{sec:test_environment}). The third dimension is the execution and evidence status, which states what was actually achieved (Section \ref{subsec:evidence_status}). The three dimensions are kept small on purpose, so that the classification supports traceability without becoming unmanageable.

\subsection{Evidence and Execution Status}
\label{subsec:evidence_status}
A single pass or fail label is insufficient for a security test. The methodology therefore records four independent attributes for each test case:
\begin{itemize}
	\item \textbf{Execution status:} performed, partially performed, not performed, not applicable, blocked by an environmental or tool constraint, or unclear.
	\item \textbf{Evidence quality:} raw evidence available, report available, narrative note only, or no evidence available.
	\item \textbf{Applicability:} applicable, applicable with adaptation, not applicable to the device, or not applicable to the topology. A test is not applicable to the topology when the laboratory network cannot provide the required position, for example a test that depends on the NV2 or NV3 variant.
	\item \textbf{Assessment outcome:} requirement supported, partially supported, not supported, or inconclusive.
\end{itemize}

The attributes are independent. A test can be executed yet inconclusive, or not applicable without being failed. A test may only support a requirement when its evidence quality is sufficient. A reported execution without a stored artifact raises the execution status, but it does not by itself support a requirement.






\section{Design of the Security Test Environment}
\label{sec:test_environment}
Security testing can disturb an industrial network. Active scanning or malicious input can interrupt communication and, in the worst case, affect a safety function. In Operational Technology, availability and safety hold the highest priority. Security tests must therefore never run in a production environment. A dedicated and isolated test environment is a mandatory requirement, in line with NIST SP 800-82 \cite[p. 2]{NIST.SP.800-82r3}.

This section defines the generic requirements for the environment, a reference zone design, and the network access variants that determine which tests are feasible.

\subsection{Generic Requirements for the Test Environment} 
\label{subsec:architectural_blueprint}
The environment must satisfy four requirements. It must be isolated from production networks. It must protect the safety communication, so that no test degrades the safety function \cite[p. 14]{prEN50742_2025}. It must keep the non-deterministic diagnostic path separate from the deterministic fieldbus path, so that load on the HTTP interface cannot introduce latency into the safety cycle. It must allow the controlled export of evidence without exposing the safety path.

\subsection{Reference Zone Design} 
\label{subsec:isolation_strategies} 
A complete environment that supports every planned test can be described as a set of functional zones. This reference design serves as a target and as a benchmark for the realised environment.

\begin{itemize}
	\item \textbf{Safety Control Zone:} a fail-safe controller that generates and receives PROFIsafe telegrams toward the SUT. It represents the trusted counterpart and remains under the tester's exclusive control.
	\item \textbf{Gateway Zone:} the SUT, which exposes the PROFINET and PROFIsafe field interface, the IoT-Core HTTP interface, and the physical configuration interface.
	\item \textbf{OT Measurement Path:} a position that permits passive capture of fieldbus traffic and, where required, controlled injection.
	\item \textbf{IT Test Path:} a position for HTTP and API testing, decoupled from the fieldbus path to avoid test-induced jitter.
	\item \textbf{Physical Access Lane:} a supervised procedure for direct interaction with the switches and the tamper-evident seals.
\end{itemize}

The realised environment does not implement every zone. The concrete, as-built topology and its limitations are documented in Chapter \ref{chap:implementation_results_evaluation}.

\subsection{Network Access Variants} 
\label{subsec:hybrid_approach} 
The feasibility of a test depends on the position of the test system on the communication path. The methodology defines three network access variants. Each variant provides a different level of access.

\begin{itemize}
	\item \textbf{NV1, direct access:} The test system communicates as a regular participant with the SUT and the controller over the switch. NV1 supports all IP-based tests, including the IoT-Core HTTP services, and any test in which the test system acts as an unexpected communication partner.
	\item \textbf{NV2, passive observation:} A switch mirror or a network tap makes traffic visible that is not addressed to the test system, such as the cyclic PROFINET and PROFIsafe communication between the controller and the SUT. NV2 requires a switch that supports and is configured for port mirroring. Address resolution spoofing is not a substitute, because the cyclic traffic uses Ethernet-level addressing (EtherType 0x8892 and DCP) rather than IP and ARP.
	\item \textbf{NV3, active in-path manipulation:} The test system is inserted inline between the SUT and the switch and operates as a transparent bridge. NV3 enables the controlled suppression, delay, or replacement of individual frames in transit.
\end{itemize}

Only NV1 was realised in this thesis. NV2 requires a confirmed and configured mirror port, and NV3 requires a physical inline insertion. Both remain part of the methodology as design variants and as future validation steps. A test that depends on NV2 or NV3 is therefore specified but not executed. This separation keeps the methodology complete and prevents an unsupported claim about passive observation or active manipulation.



\section{Test Execution Framework and Governance}
\label{sec:execution_governance}
Once the environment and the test cases are defined, the execution phase begins. This section describes how tests are conducted, how existing functional evidence relates to security testing, and how organizational responsibility is assigned.

\subsection{Test Execution and Tooling}
\label{subsec:execution_tooling}
Security testing exercises an object under defined conditions and compares the actual behaviour with the expected behaviour \cite[p. ES-1]{nist_sp800_115}. The SUT is evaluated as a compiled product without source access. The methodology therefore uses dynamic black-box testing, also known as Dynamic Application Security Testing. Static source analysis is out of scope, as stated in Section \ref{subsec:methodological_scope} \cite[p. 6777]{aydos_security_2022}.

The execution uses Kali Linux as the standardised operating environment. Several options were considered. A general-purpose Linux distribution with individually installed tools offers flexibility, but it increases setup effort and reduces reproducibility. Commercial security suites offer integrated reporting, but their closed nature limits transparency and independent reproduction. Comparable open security distributions, such as Parrot OS, provide a similar toolset. Kali Linux was selected because it bundles a maintained and pre-configured set of open-source auditing tools, which improves reproducibility and removes the need to develop custom exploit code \cite{kali_whatis}. Its use of the same platform as the company framework is a secondary benefit that supports transfer into the industrial process, not the primary reason for the selection. The tools are grouped by function: network discovery and service enumeration, HTTP and API interception, credential testing, and load or robustness testing \cite[p. A-1]{nist_sp800_115}. A tool is used only after the applicability assessment of Section \ref{subsec:company_framework} confirms that the target exposes the assumed interface. The concrete tool bindings, commands, and parameters are reported with the results in Chapter \ref{chap:implementation_results_evaluation}.

The engineering tool for the safety configuration, TIA Portal, is used only as a supporting instrument, for example to read identification and maintenance data or to observe the safety qualifier. It is not treated as a security testing tool.

\subsection{Relationship to Existing Functional and Safety Tests}
\label{subsec:functional_overlap}
The SUT is a safety device. Some security-relevant properties are therefore already exercised by functional or safety validation. An existing test does not automatically prove cybersecurity compliance. Safety validation examines behaviour under random faults, whereas a security test requires an adversarial perspective and a security-specific acceptance criterion.

For each overlap, the methodology assigns one of three decisions. Three decisions are used because a binary reuse choice would hide the common middle case in which a safety test exercises the mechanism but not the adversarial criterion. A test is reusable when it already covers the adversarial objective. A test is reusable with security-specific acceptance criteria when it exercises the mechanism but not the attack perspective. A dedicated security test is required when no functional test covers the property. For example, the passivation of the safety channel on communication loss is validated for random faults. It can be reused for the corresponding security objective only after the acceptance criterion is extended to a deliberately induced disruption.


\subsection{Lifecycle Integration and RACI Governance}
\label{subsec:lifecycle_governance}

The verification activities defined in this framework are not executed in isolation. They must anchor to defined stages of the product lifecycle. This ensures that test cases are generated, executed, and evaluated at determinable points in the development process. Secure Software Development Life Cycle (SSDLC) models embed security-related activities into every phase of a conventional development process, rather than confining them to a single stage prior to release \cite{ibm_ssdlc_2025, geeksforgeeks_ssdlc_2026}.

To illustrate this integration, Figure \ref{fig:secure-system-development-lifecycle-main-overview} in Appendix \ref{app:SecureDevelopmentLifecycle} consolidates SSDLC and DevSecOps phase models into a single overview. Consistent with the methodological scope defined in Section \ref{subsec:methodological_scope}, this methodology addresses only the verification-relevant subset of such a lifecycle. Upstream design stages and downstream deployment stages are treated as boundary conditions that supply inputs to, or consume outputs from, the verification stages.

To ensure strict organizational accountability, responsibility for the testing phases is governed through a Responsible-Accountable-Consulted-Informed (RACI) matrix. This established management method ensures exactly one role is accountable for a given activity \cite{cybersierra_security_raci_2026, su13041806}. Six generic roles are distinguished for this purpose \cite[p. 6]{Schmidt2026CybersecurityTestProcess}:

\begin{itemize}
	\item \textbf{Security Engineering:} Holds technical accountability for test scope and execution integrity.
	\item \textbf{System/Requirements Engineering:} Consulted where a requirement or asset boundary is affected.
	\item \textbf{Test Engineering:} Responsible for procedure preparation, execution, and verdict assignment.
	\item \textbf{Software/Firmware Development:} Consulted on findings and tasked with implementing corrective actions.
	\item \textbf{Quality/Compliance Management:} Responsible for coverage review and evidence governance.
	\item \textbf{Release Authority:} Accountable for the final release decision.
\end{itemize}

Table \ref{tab:raci_matrix_verification} defines the distribution of these responsibilities across the verification phases.

\begin{table}[htbp]
	\caption{RACI Matrix for the Verification Lifecycle (adapted from \cite{cybersierra_security_raci_2026})}
	\label{tab:raci_matrix_verification}
	\centering
	\begin{tabularx}{\textwidth}{l c c c c c c}
		\toprule
		\textbf{Activity} & \makecell{\textbf{Sec.}\\\textbf{Eng.}} & \makecell{\textbf{Sys/Req}\\\textbf{Eng.}} & \makecell{\textbf{Test}\\\textbf{Eng.}} & \makecell{\textbf{SW/FW}\\\textbf{Dev.}} & \makecell{\textbf{QA/}\\\textbf{Compl.}} & \makecell{\textbf{Release}\\\textbf{Auth.}} \\ \midrule
		Design & A & C & R & I & I & I \\ \addlinespace
		Preparation & C & I & A/R & C & I & I \\ \addlinespace
		Execution & A & I & R & C & I & I \\ \addlinespace
		Evidence/Eval. & C & I & R & C & A & I \\ \addlinespace
		Release Decision & I & I & I & I & C & A/R \\
		\bottomrule
	\end{tabularx}
\end{table}

As illustrated in Table \ref{tab:raci_matrix_verification}, accountability shifts between phases. It follows the organizational ownership of a phase's primary deliverable rather than a fixed functional assignment \cite{cybersierra_security_raci_2026}. Distinguishing the role responsible for executing a verification activity from the role accountable for closing it out reflects the practical division of labor documented in secure software engineering \cite[p. 5]{219394}. Ultimately, these role assignments directly support IEC 62443-4-1 Practice 5 (Security Verification and Validation Testing). This standard mandates a rigorous process demonstrating that the product's security functions meet all specified requirements \cite[pp. 37]{DIN_EN_IEC_62443_4_1_2018}.

Beyond a single release cycle, the governance model must also account for the maintenance of the methodology itself. Consistent with the iterative application of TARA (see Section \ref{subsec:tara}), the requirement baseline and the associated test cases require re-evaluation under specific triggers. These triggers include the introduction of a new product variant, revisions to normative documents (Regulation (EU) 2023/1230, prEN 50742, or IEC 62443-4-2), or the disclosure of a post-release vulnerability. Under the RACI model, initiating this re-evaluation falls under the consulting role of System/Requirements Engineering, while Quality/Compliance Management remains accountable for updating the technical documentation.



\section{Evaluation Metrics and Compliance Assessment}
\label{sec:evaluation_metrics}
The evaluation links test results to requirement verification. It works on two levels. The first level assigns a verdict to each individual test case. The second level aggregates the verdicts into indicators for the requirement fulfilment and the methodological quality \cite[p. 144, 217]{Witte2019}. Two indicators address the thesis objective directly. The requirement compliance rate reports the fulfilment of the derived obligations, and the threat mitigation coverage reports the effectiveness of the tested security functions. Two further indicators address the quality of the evidence, which the technical documentation under Regulation (EU) 2023/1230 requires.

\subsection{Test Case and Requirement Verdicts}
\label{subsec:verdicts}
A verdict is assigned from the acceptance criterion and the four status attributes of Section \ref{subsec:evidence_status}. Four scored verdicts are distinguished \cite[p. 1553]{11050621} \cite[p. 23]{richier2011security}:

\begin{itemize}
	\item \textbf{PASS:} The test case was executed, all mandatory acceptance criteria are satisfied, and the evidence is sufficient to demonstrate the security function.
	\item \textbf{PARTIAL:} The test case was executed and the mandatory criteria are satisfied, but a non-critical criterion remains unfulfilled or the mechanism showed minor degradation.
	\item \textbf{FAIL:} The test case was executed and a mandatory security function was bypassed.
	\item \textbf{NOT APPLICABLE:} The assumed interface or function is absent, or the topology does not support the test. This decision requires a documented technical justification and is not a failure.
\end{itemize}

Two further states are not scored. A test whose evidence is insufficient is \textit{inconclusive}. A test that could not run because of an environmental or tool constraint is \textit{blocked}. Only a scored verdict contributes to the aggregated indicators. A reported execution without a stored artifact therefore remains inconclusive.

The verdicts are aggregated per requirement. A requirement is verdicted FAIL if any associated test case fails. It is verdicted PASS only if every associated and applicable test case passes with sufficient evidence.



\subsection{Core Framework Metrics}
\label{subsec:framework_metrics}
Four Key Performance Indicators evaluate the security posture of the gateway and the quality of the test execution. The indicators use the ratio-based style established for test-process metrics \cite[p. 145]{Witte2019}. They are computed over the \textit{applicable} requirements, so that documented exclusions do not skew the result. Where a large part of the catalogue is specified but not executed, the indicators are reported over the performed subset, and the number of executed cases is stated alongside the ratio.

\textbf{Requirement Compliance Rate ($RCR$)} is the primary legal indicator. It measures the strict fulfillment of the derived normative obligations from Regulation (EU) 2023/1230 and IEC 62443-4-2 (based on \cite[p. 223]{Witte2019}).
\begin{equation}
	RCR = \frac{\text{Applicable requirements verdicted PASS}}{\text{Total applicable requirements}}
\end{equation}

\textbf{Threat Mitigation Coverage ($TMC$)} evaluates the technical security posture from an engineering perspective. Referencing the STRIDE threat model defined in Section \ref{subsec:stride}, this metric calculates the proportion of identified, asset-specific threat vectors that were successfully blocked by the tested security functions.
\begin{equation}
	TMC = \frac{\text{Successfully mitigated STRIDE threats}}{\text{Total modeled STRIDE threats for tested assets}}
\end{equation}

\textbf{Test Effectiveness Ratio ($TER$)} measures the methodological quality of the test execution. A test is considered highly effective if it yields a definitive empirical outcome (\textit{PASS} or \textit{FAIL}) rather than failing due to testbed errors, tool crashes, or inconclusive system behavior.
\begin{equation}
	TER = \frac{\text{Executed test cases yielding a definitive PASS or FAIL}}{\text{Total executed test cases}}
\end{equation}

\textbf{Evidence Traceability Score ($ETS$)} assesses audit readiness. It indicates the proportion of executed tests supported by complete, traceable digital artifacts (e.g., PCAP files, system logs). This metric directly addresses the strict evidentiary obligations for the technical documentation under Article 10(2) of the Machinery Regulation \cite{EU2023_1230}.
\begin{equation}
	ETS = \frac{\text{Executed test cases with comprehensive digital evidence}}{\text{Total executed test cases}}
\end{equation}

\subsection{Reliability, Repeatability, and Validity}
\label{subsec:reliability_validity}
The methodology addresses reliability at three levels. Repeatability is supported by the two-level test specification, which records the exact procedure and evidence for each test. Reproducibility is supported by the use of a standardised tool environment and by the applicability assessment, which documents why a tool applies to the target. Traceability is supported by the requirement chain of Section \ref{subsec:traceability_model}.

Several factors limit the validity of the results. The evaluation covers a single System Under Test, so the results do not generalise to other devices or fieldbuses. The practical demonstration is limited to the NV1 network position, so claims that depend on passive observation or active manipulation remain unverified. The cyclic safety communication is not independently monitored, so the methodology does not claim that the safety function is unaffected by a test. prEN 50742 is a draft standard, so the derivation supports the demonstration of conformity without asserting certification.

For these reasons, the artifact is best described as a prototype methodology with partial practical validation. The methodology is fully specified across all three network access variants, and it is demonstrated for the tests that the NV1 position supports. The validation of the NV2 and NV3 tests remains future work.




